const TOOLTIP_DESCRIPTIONS = {
  initialQuality: 'Image quality from 0.1 (low) to 1.0 (high). Lower values produce smaller files.',
  sizePresets: 'Select the icon sizes to include in the ICO file. Common choices are 32x32, 48x48, and 128x128. 256x256 is stored as PNG for better compression.'
};

export default TOOLTIP_DESCRIPTIONS;
